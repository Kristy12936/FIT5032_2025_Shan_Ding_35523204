import './assets/main.css'
// import './style.css'  //  注释掉自定义样式（Activity 3 用的）

import 'bootstrap/dist/css/bootstrap.min.css'  //  新增：引入 Bootstrap 样式

import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')